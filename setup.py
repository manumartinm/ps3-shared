from setuptools import setup, find_packages

setup(
    name="ps3-shared",
    version="1.0.0",
    description="Shared code for the ps3 backend",
    packages=find_packages(),
    python_requires=">=3.12",
)
